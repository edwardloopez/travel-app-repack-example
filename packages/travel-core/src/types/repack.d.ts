/** Re.Pack ScriptManager types referenced from @callstack/repack dist. */
declare namespace RepackRuntimeGlobals {
  type ScriptLocator = {
    url: string;
    cache?: boolean;
    retry?: number;
    retryDelay?: number;
  };
}
